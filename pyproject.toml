[project]
name = "poetry-github-application-demo"
version = "0.1.0"
description = "This is a sample code to test poetry and github actions"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest==8.3.4"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
